import 'package:flutter/material.dart';

final class AppPadding {
  static const EdgeInsets pagePadding = EdgeInsets.all(20);
  static const EdgeInsets segmentPadding = EdgeInsets.all(10);
  static const EdgeInsets scrollPagePadding = EdgeInsets.symmetric(
    horizontal: 20,
    vertical: 0,
  );
}
